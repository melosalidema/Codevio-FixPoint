"""S1-S16 security and reliability evaluation matrix.

Import the runner directly (``from app.evals.runner import run_all``) so the
package stays side-effect free. Run it with ``python -m app.evals.runner``.
"""
